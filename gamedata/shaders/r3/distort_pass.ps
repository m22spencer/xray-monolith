#include "distortion.h"

float4 m_hud_params;
float isSVPFrame() { return m_hud_params.w > 0.5; }

#define SVP_SCALE_FACTOR 2.0

float4 main(v2p_TL I) : SV_Target
{
    return s_image.Sample(smp_base, distortion_tc(I.Tex0, isSVPFrame() ? SVP_SCALE_FACTOR : 1.0));
} 
